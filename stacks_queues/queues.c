int enqueue(int* A, int item)
{
  return 0;
}

int dequeue(int* A)
{
  return 0;
}

int size(int* A)
{
  // r-f will be incorrect in some case where it could be negative. 
  // N-f+r
}

int isEmpty(int* A)
{
  // f=r queue is empty
}

/* Queues using Linked List. Arrays are fixed. 

In a queue, removal happens at the front
*/

/* Double ended queue - support insertion and deletion at both ends

Doubly Linked List
next and prev
head and tail pointer
*/
